Run one or more of the following to generate the corresponding distance matrices:
1) `make rank_joao_matrix`
2) `make rank_lucas_matrix`
3) `make dcj_matrix`

(1) and (2) are essentially equivalent, but implemented differently.
